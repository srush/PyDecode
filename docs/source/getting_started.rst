---------------
Setup
---------------

Installation
~~~~~~~~~~~~

Build the C++ library::

    $ scons

Build the Python extension::

    $ python setup.py config
    $ python setup.py build
    $ python setup.py install

Dependencies
~~~~~~~~~~~~

- Boost
- Pandas (optional)
- NetworkX (optional)
- Nose (optional)
